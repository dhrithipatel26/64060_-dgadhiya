#Importing libaries
library(ggplot2)
library(dplyr)

#reading the data
data <- read.csv("Employers_data.csv", stringsAsFactors = TRUE)

# Quantitative variables
summary(data$Age)
summary(data$Salary)
summary(data$Experience_Years)

# Categorical variables
table(data$Gender)
table(data$Department)
table(data$Education_Level)

# Transform a variable
## Example: log-transform Salary
data$Log_Salary <- log(data$Salary + 1)

# Plot quantitative variable
## Histogram of Age
ggplot(data, aes(x = Age)) +
  geom_histogram(binwidth = 5, fill = "steelblue", color = "black") +
  labs(title = "Histogram of Employee Age", x = "Age", y = "Count")

# Scatterplot
## Scatterplot of Experience vs Salary
ggplot(data, aes(x = Experience_Years, y = Salary, color = Department)) +
  geom_point(alpha = 0.6) +
  labs(title = "Experience vs Salary by Department", x = "Experience (Years)", y = "Salary")